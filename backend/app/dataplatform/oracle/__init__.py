"""Oracle (omega) source access: connectivity, metadata, boundaries, extraction.

Everything here operates strictly read-only through the dedicated extraction
identity (ORA-001/ORA-002); no code in this package can mutate the source.
"""
