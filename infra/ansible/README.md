# SmartForge Ansible (scaffold)

Edge-gateway / bare-metal provisioning direction (spec §10): install container
runtime, deploy the edge agent that bridges OPC/PLC data to the API's telemetry
ingest endpoint, and configure node-local Prometheus exporters near factory
equipment for site-to-site connectivity.
